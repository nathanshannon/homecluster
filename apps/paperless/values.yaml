paperless:
  ingress:
    main:
      enabled: false
      ingressClassName: nginx
      hosts:
        - host: &host paperless.home.nathanshannon.net
          paths:
            - path: /
              pathType: Prefix