radarr:
  ingress:
    main:
      enabled: false
      ingressClassName: nginx
      hosts:
        - host: &host radarr.home.nathanshannon.net
          paths:
            - path: /
              pathType: Prefix 