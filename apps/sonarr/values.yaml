sonarr:
  ingress:
    main:
      enabled: false
      ingressClassName: nginx
      hosts:
        - host: &host sonarr.home.nathanshannon.net
          paths:
            - path: /
              pathType: Prefix 